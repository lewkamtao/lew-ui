import { convertEmits } from 'docs/lib/utils'
import { checkboxGroupEmits } from 'lew-ui'

export default {
  title: 'Emits(CheckboxGroup)',
  columnsKey: 'emits',
  orderNum: 99,
  data: convertEmits(checkboxGroupEmits),
}
