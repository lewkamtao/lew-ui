---
description: # Vue 组件样式取舍与性能优化实战指南 本文是 **Vue 组件开发的一站式优化手册**，核心解决 `computed style` 与 `class` 的选型难题，提炼「数值计算用 style、状态类型用 class」的黄金原则，覆盖样式设计、组件性能、打包优化全流程。  内容包含**样式取舍判断标准**、**组件库开发范式**、**CSS Variables 最佳实践**，同时提供 Props 设计、Slot 渲染、防抖节流、Tree-shaking、动画性能调优的可落地方案，配套性能测试方法、调试指令与组件开发检查清单，适用于 Vue 业务组件开发、组件库维护及前端性能优化场景，助力写出高可维护、高性能的 Vue 代码。
alwaysApply: false
---
### ✅ 什么时候「必须」用 computed style

#### 1. 样式值是"数学结果"

```typescript
// ✅ 进度条
const progressStyle = computed(() => ({
  width: `${percent.value}%`
}))

// ✅ 拖拽位移
const dragStyle = computed(() => ({
  transform: `translate3d(${x.value}px, ${y.value}px, 0)`
}))

// ✅ 动态坐标
const positionStyle = computed(() => ({
  left: `${coords.x}px`,
  top: `${coords.y}px`
}))
```

**适用场景**：
- 进度条、拖拽、动画
- Canvas、时间轴、坐标系统
- 任何需要实时计算像素值的场景

---

#### 2. CSS 无法提前穷举

```typescript
// ✅ 用户输入的颜色
const style = computed(() => ({
  backgroundColor: userColor.value
}))

// ✅ 接口返回的尺寸
const style = computed(() => ({
  width: any2px(apiWidth.value),
  height: any2px(apiHeight.value)
}))
```

**适用场景**：
- 颜色、尺寸、位置是用户输入或接口返回
- 不可能写 1000 个 class 来穷举

---

#### 3. 高性能动画（transform / translate）

```typescript
// ✅ 使用 transform 的动画
const style = computed(() => ({
  transform: `translate3d(${x.value}px, ${y.value}px, 0)`
}))
```

**原因**：transform 触发 GPU 加速，用 class 反而更慢更乱

---

### ❌ 什么时候「一定不要」用 computed style

#### 1. 状态切换类样式

```typescript
// ❌ 不推荐
const style = computed(() => ({
  color: isActive.value ? '#1677ff' : '#999'
}))

// ✅ 推荐
const classes = computed(() => ({
  'is-active': isActive.value
}))
```

```vue
<div :class="classes" />
```

```css
.is-active {
  color: var(--lew-color-primary);
}
```

**原因**：
- 样式语义丢失
- 难以维护
- 设计系统无法复用
- 主题系统无法接管

---

#### 2. 和设计规范强绑定的样式

```typescript
// ❌ 不要这样写按钮样式
const buttonStyle = computed(() => ({
  backgroundColor: props.type === 'primary' ? '#1677ff' : '#fff',
  color: props.type === 'primary' ? '#fff' : '#333',
  border: props.disabled ? '1px solid #d9d9d9' : 'none'
}))

// ✅ 正确写法
const buttonClass = computed(() => [
  'lew-button',
  `lew-button-${props.type}`,
  {
    'is-disabled': props.disabled,
    'is-loading': loading.value
  }
])
```

**应该用 class 的样式**：
- 按钮主色、hover、focus
- 禁用态、错误态
- 所有和设计规范相关的样式

---

#### 3. 组件库的可复用样式

> **组件库里滥用 style = 非常不专业**

```typescript
// ❌ 这会导致：
// - 主题系统无法切换
// - Dark mode 崩溃
// - 用户无法覆盖样式
// - Tailwind / CSS Variables 无法接管

const style = computed(() => ({
  padding: props.size === 'large' ? '12px 24px' : '8px 16px',
  fontSize: props.size === 'large' ? '16px' : '14px'
}))

// ✅ 正确做法
const classes = computed(() => [
  'lew-button',
  `lew-button--${props.size}`
])
```

```css
.lew-button--large {
  padding: var(--lew-button-padding-large);
  font-size: var(--lew-button-font-size-large);
}

.lew-button--medium {
  padding: var(--lew-button-padding-medium);
  font-size: var(--lew-button-font-size-medium);
}
```

---

### 🔥 组件库里的「黄金范式」

#### 推荐结构

```typescript
// 1. class 表达状态和类型
const classes = computed(() => [
  'lew-button',
  `lew-button--${props.type}`,
  `lew-button--${props.size}`,
  {
    'is-loading': loading.value,
    'is-disabled': disabled.value,
    'is-round': props.round,
    'is-circle': props.circle,
    'is-text': props.text,
    'is-block': props.block
  }
])

// 2. 只把「数值型」动态值放到 style
const style = computed(() => {
  const result: Record<string, string | undefined> = {}
  
  // 动态宽度
  if (props.block) {
    result.width = '100%'
  } else if (props.width) {
    result.width = any2px(props.width)
  }
  
  // CSS 变量（用于深度定制）
  if (props.customColor) {
    result['--button-color'] = props.customColor
  }
  
  return result
})
```

```vue
<button :class="classes" :style="style">
  <slot>{{ text }}</slot>
</button>
```

```css
.lew-button {
  /* 使用 CSS 变量作为桥梁 */
  background-color: var(--button-color, var(--lew-color-primary));
}

.lew-button--primary { ... }
.lew-button--danger { ... }
.is-loading { ... }
.is-disabled { ... }
```

---

#### CSS Variables 作为桥梁（强烈推荐）

```typescript
// ✅ 使用 CSS 变量传递动态值
const style = computed(() => ({
  '--gap': any2px(props.gap),
  '--radius': any2px(props.radius),
  '--duration': `${props.duration}ms`
}))
```

```vue
<div class="lew-container" :style="style">
  <slot />
</div>
```

```css
.lew-container {
  gap: var(--gap, 8px);
  border-radius: var(--radius, 4px);
  transition-duration: var(--duration, 300ms);
}
```

**优势**：
- ✅ 样式逻辑在 CSS 中（语义清晰）
- ✅ 支持主题系统
- ✅ 用户可以通过 CSS 覆盖
- ✅ 性能更好（浏览器优化）

---

### 一眼判断用哪个（实用口诀）

#### 判断标准 1：你是不是在"算值"？

```typescript
// 在算值 → style
const style = computed(() => ({
  width: `${baseWidth.value + padding.value * 2}px`  // ✅
}))

// 不在算值 → class
const classes = computed(() => ({
  'is-active': isActive.value  // ✅
}))
```

---

#### 判断标准 2：这个样式有没有"名字"？

```typescript
// 有名字（active / disabled / error）→ class
const classes = computed(() => ({
  'is-active': isActive.value,      // ✅ 有语义
  'is-disabled': isDisabled.value,  // ✅ 有语义
  'is-error': hasError.value        // ✅ 有语义
}))

// 没有名字（20px、37%、x+10）→ style
const style = computed(() => ({
  left: `${x.value + 10}px`,        // ✅ 纯数值
  width: `${percent.value}%`        // ✅ 纯数值
}))
```

---

#### 判断标准 3：CSS 配置对象是否应该用 class？

```typescript
// ❌ 不好：静态配置对象用 computed style
const STATUS_PLACEMENT_CONFIG = {
  "top-left": { top: "-0.05rem", left: "-0.05rem" },
  "top-right": { top: "-0.05rem", right: "-0.05rem" },
  // ...
}

const dotStyle = computed(() => ({
  ...STATUS_PLACEMENT_CONFIG[props.placement],  // ❌ 静态配置不应该用 style
  backgroundColor: STATUS_COLOR_CONFIG[props.status],
}))

// ✅ 更好：静态配置用 CSS class，只把动态值放 style
const statusDotClass = computed(() => 
  `lew-avatar-status-dot--${props.shape}-${props.placement}`
)

const statusDotStyle = computed(() => ({
  backgroundColor: STATUS_COLOR_CONFIG[props.status],  // ✅ 只保留动态值
  width: any2px(dotSize),  // ✅ 动态计算的值
}))
```

```css
/* ✅ CSS 中定义静态配置 */
.lew-avatar-status-dot--circle-top-left {
  top: -0.05rem;
  left: -0.05rem;
}

.lew-avatar-status-dot--circle-top-right {
  top: -0.05rem;
  right: -0.05rem;
}
```

**原则**：
- **静态配置**（位置、固定尺寸）→ 用 CSS class
- **动态值**（颜色、计算尺寸）→ 用 computed style
- **组合使用**：class 控制结构，style 控制动态值

---

### 性能对比

#### computed style 的性能成本

```typescript
// ❌ 每次 isActive 变化都会触发内联样式更新
const style = computed(() => ({
  color: isActive.value ? '#1677ff' : '#999',
  backgroundColor: isActive.value ? '#e6f7ff' : '#fff',
  borderColor: isActive.value ? '#1677ff' : '#d9d9d9'
}))

// ✅ 只是切换 class，浏览器使用缓存的样式
const classes = computed(() => ({
  'is-active': isActive.value
}))
```

**性能差异**：
- **computed style**：需要重新解析、计算、应用样式
- **class**：浏览器直接使用 CSSOM 缓存（快 10-100 倍）

---

### 总结（一句人话）

| 用途 | 使用方式 | 典型场景 |
|------|---------|---------|
| **表达状态** | class | active / disabled / loading / error |
| **表达类型** | class | primary / danger / large / small |
| **设计系统** | class | 所有设计规范定义的样式 |
| **计算结果** | style | 进度条、拖拽、动画 |
| **动态数值** | style | 用户输入、API 返回的值 |
| **桥梁** | CSS Variables | 可定制的数值参数 |

**组件库原则**：
- ✅ **class 为主，style 为辅**
- ✅ **状态用 class，数值用 style**
- ✅ **CSS Variables 是最好的桥梁**

---

## 组件设计优化

### 1. Props 设计

```typescript
// ✅ 使用基础类型而非对象（减少响应式开销）
// ❌ 不好
defineProps<{
  config: {
    size: string
    color: string
    disabled: boolean
  }
}>()
// ✅ 更好
defineProps<{
  size: string
  color: string
  disabled: boolean
}>()

// ✅ 避免 props 传递大对象
// ❌ 不好
defineProps<{
  data: LargeDataObject  // 整个对象都会被 Vue 处理
}>()
// ✅ 更好：只传必要的数据
defineProps<{
  dataId: string
  dataName: string
}>()
```

### 2. Slots 优化

```vue
<script setup lang="ts">
// ✅ 检测 slot 是否存在，避免不必要的渲染
const slots = useSlots()
const hasFooter = computed(() => !!slots.footer)
</script>

<template>
  <!-- ❌ 不好：总是渲染包裹元素 -->
  <div class="footer-wrapper">
    <slot name="footer" />
  </div>

  <!-- ✅ 更好：有内容才渲染 -->
  <div v-if="hasFooter" class="footer-wrapper">
    <slot name="footer" />
  </div>
</template>
```

### 3. 事件处理

```vue
<script setup lang="ts">
// ✅ 使用函数引用而非内联箭头函数（避免重复创建）
const handleClick = (e: MouseEvent) => {
  emit('click', e)
}

// ✅ 防抖/节流
import { useDebounceFn, useThrottleFn } from '@vueuse/core'

const debouncedSearch = useDebounceFn((value: string) => {
  // 搜索逻辑
}, 300)
</script>

<template>
  <!-- ❌ 不好：每次渲染都创建新函数 -->
  <button @click="() => emit('click')">Click</button>

  <!-- ✅ 更好：使用函数引用 -->
  <button @click="handleClick">Click</button>

  <!-- ✅ 简单情况可以内联 emit -->
  <button @click="emit('click', $event)">Click</button>
</template>
```

## 样式性能优化

### 1. CSS 作用域

```scss
// ✅ 使用 scoped 避免全局污染
<style lang="scss" scoped>
.lew-button {
  // 组件样式
}
</style>

// ❌ 避免深层选择器嵌套（影响性能）
// 不好：
.lew-table {
  .lew-table-row {
    .lew-table-cell {
      .lew-table-cell-content {
        // 4 层嵌套
      }
    }
  }
}
// 更好：
.lew-table-cell-content {
  // 直接定位
}
```

### 2. CSS 变量性能

```scss
// ✅ 在根元素定义变量
.lew-button {
  --lew-button-bg: transparent;
  --lew-button-color: var(--lew-color-primary);
  
  background: var(--lew-button-bg);
  color: var(--lew-button-color);
}

// ❌ 避免过度使用 calc()
// 不好：
width: calc(calc(100% - 20px) / 3 + 5px);
// 更好：预计算或使用 CSS Grid
```

### 3. 动画性能

```scss
// ✅ 只动画 transform 和 opacity
.lew-button {
  transition: transform 0.2s, opacity 0.2s;
  
  &:hover {
    transform: translateY(-2px);
    opacity: 0.8;
  }
}

// ❌ 避免动画布局属性
// 不好：
transition: width 0.3s, height 0.3s, top 0.3s;

// ✅ 使用 will-change 提示浏览器（谨慎）
.lew-dropdown {
  will-change: transform, opacity;
}
```

## 打包体积优化

### 1. Tree-shaking 友好

```typescript
// ✅ 使用具名导出
export { LewButton }
export { LewInput }

// ❌ 避免整体导出
// export default { LewButton, LewInput }

// ✅ 避免导入整个 lodash
// ❌ import _ from 'lodash'
// ✅ import { debounce } from 'lodash-es'

// ✅ 条件导入
if (isDev) {
  const devTool = await import('./devTool')
}
```

### 2. 依赖管理

```typescript
// ✅ 使用项目已有的依赖
import { useDebounce } from '@vueuse/core'  // ✅ 项目已安装

// ❌ 避免引入重复功能的包
// import debounce from 'awesome-debounce'  // ❌ 新增依赖

// ✅ 对于简单功能，考虑自己实现
export function debounce<T extends (...args: any[]) => any>(
  fn: T,
  delay: number
): T {
  let timer: ReturnType<typeof setTimeout>
  return ((...args) => {
    clearTimeout(timer)
    timer = setTimeout(() => fn(...args), delay)
  }) as T
}
```

### 3. 代码分割

```typescript
// 组件库的 index.ts
// ✅ 支持按需导入
export { LewButton } from './button'
export { LewInput } from './input'

// 用户可以：
import { LewButton } from 'lew-ui'  // 只打包 Button
```

## 检查清单

实现组件时确认：
- [ ] 避免了不必要的响应式数据
- [ ] computed 有合理的缓存策略
- [ ] 大列表使用了虚拟滚动
- [ ] 低频/重型组件使用异步加载
- [ ] 样式动画只使用 transform/opacity
- [ ] 没有引入不必要的依赖
- [ ] 支持 tree-shaking
- [ ] 开发环境有性能提示
- [ ] 组件体积符合目标（< 10KB）
- [ ] **状态切换使用 class（不是 computed style）**
- [ ] **只在计算数值时使用 style**
- [ ] **设计规范样式都在 CSS 中（不在 JS）**
- [ ] **使用 CSS Variables 传递动态参数**

## 性能调试工具

```bash
# 分析打包体积
pnpm run analyze

# 运行性能测试
pnpm run test:performance

# TypeScript 类型检查
pnpm run typecheck
```

---
