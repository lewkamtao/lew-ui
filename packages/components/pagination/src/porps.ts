export const paginationProps = {
    total: {
        type: Number,
        default: 100,
    },
    pageNum: {
        type: Number,
        default: 1,
    },
    pageSize: {
        type: Number,
        default: 20,
    },
    pageShowSize: {
        type: Number,
        default: 3,
    },
    background: {
        type: Boolean,
        default: false,
    },
    round: {
        type: Boolean,
        default: false,
    },
};
