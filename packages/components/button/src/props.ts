export const buttonProps = {
    type: {
        type: String,
        default: 'primary',
    },
    size: {
        type: String,
        default: 'medium',
    },
    loading: {
        type: Boolean,
        default: false,
    },
    disabled: {
        type: Boolean,
        default: false,
    },
    isIcon: {
        type: Boolean,
        default: false,
    },
    round: {
        type: Boolean,
        default: false,
    },
    isText: {
        type: Boolean,
        default: false,
    },
};
