import { menuTreeItemProps } from 'lew-ui'
import { convertProps } from '@/lib/utils'

export default {
  title: 'Props (MenuTreeItem)',
  columnsKey: 'props',
  orderNum: 4,
  data: convertProps(menuTreeItemProps)
}
