import { descItemProps } from 'lew-ui'
import { convertProps } from '@/lib/utils'

export default {
  title: 'DescItemProps',
  columnsKey: 'props',
  orderNum: 12,
  data: convertProps(descItemProps)
}
