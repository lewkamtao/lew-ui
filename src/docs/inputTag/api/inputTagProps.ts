import { inputTagProps } from 'lew-ui';
import { convertProps } from '@/lib/utils';

export default {
    title: 'inputTagProps',
    columnsKey: 'props',
    orderNum: 10,
    data: convertProps(inputTagProps),
};
