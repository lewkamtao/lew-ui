import { checkboxGroupModel } from 'lew-ui';
import { convertProps } from '@/lib/utils';

export default {
    title: 'Model (checkbox-group)',
    columnsKey: 'model',
    orderNum: 3,
    data: convertProps(checkboxGroupModel)
};
